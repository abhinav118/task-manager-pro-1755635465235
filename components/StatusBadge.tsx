interface StatusBadgeProps {
  size?: any
  variant?: any
}

export default function StatusBadge({ size, variant }: StatusBadgeProps) {
  return (
    <div className="px-2 py-1 text-xs rounded-full font-medium">
      {/* StatusBadge component content */}
    </div>
  )
}