interface PriorityIconProps {
  size?: any
  variant?: any
}

export default function PriorityIcon({ size, variant }: PriorityIconProps) {
  return (
    <div className="w-6 h-6 rounded-full flex items-center justify-center">
      {/* PriorityIcon component content */}
    </div>
  )
}